# Minimal dependencies for Irradiation Portal NASA POWER and CAMS serving.
# Install SuSSE itself with --no-deps when using this requirements contract.
geopy>=2.4.1
numpy>=2.0.2
pandas>=2.2.3
pvlib>=0.11.2
python-dotenv>=1.0.0
requests>=2.32.3
